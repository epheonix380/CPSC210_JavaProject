package ui.elements;

import javax.swing.*;

// Represents a Label that is stylized to match the rest of the application
public class Label extends JLabel {

    // MODIFIES: This
    // REPRESENTS: text is the text that will be displayed on the label
    public Label(String text) {
        super(text);
    }
}
